---
layout: page
permalink: /publications/
title: Publications
description: publications by categories in reversed chronological order. generated by jekyll-scholar.
nav: true
nav_order: 20
---

<style>
  @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;600;700&family=Lato:ital,wght@0,400;0,600;1,400&display=swap');

  /* ── Shared design tokens ───────────────────────────── */
  :root {
    --primary:       #2e4a7a;
    --primary-light: #3a5c96;
    --accent-soft:   #c8b8f0;
    --text-main:     #333;
    --text-muted:    #666;
  }

  /* ── Section headers (h2) ───────────────────────────── */
  .page-section-heading {
    font-family: 'Roboto', sans-serif;
    font-size: 1.55rem;
    font-weight: 700;
    color: var(--primary);
    border-bottom: 2px solid var(--primary);
    padding-bottom: 6px;
    margin-top: 2.2rem;
    margin-bottom: 1rem;
    letter-spacing: 0.01em;
  }

  /* ── WIP Banner Bar (sits just above the publications list) ── */
  .wip-banner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background-color: #eef2fa;
    border: 1.5px solid var(--primary);
    border-radius: 6px;
    padding: 10px 16px;
    margin-bottom: 1.8rem;
    cursor: pointer;
    transition: background-color 0.2s;
  }
  .wip-banner:hover {
    background-color: #dce5f5;
  }
  .wip-banner-left {
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .wip-banner-icon {
    font-size: 1.1rem;
  }
  .wip-banner-label {
    font-family: 'Roboto', sans-serif;
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--primary);
    text-transform: uppercase;
    letter-spacing: 0.09em;
  }
  .wip-banner-hint {
    font-family: 'Lato', sans-serif;
    font-size: 0.78rem;
    color: var(--text-muted);
    font-style: italic;
  }
  .wip-banner-chevron {
    font-size: 0.75rem;
    color: var(--primary);
    transition: transform 0.25s;
  }
  .wip-banner.open .wip-banner-chevron {
    transform: rotate(180deg);
  }

  /* ── WIP Dropdown Panel ─────────────────────────────── */
  .wip-dropdown {
    display: none;
    background: #f7f9ff;
    border: 1.5px solid var(--primary);
    border-top: none;
    border-radius: 0 0 6px 6px;
    padding: 14px 16px;
    margin-top: -1.8rem;
    margin-bottom: 1.8rem;
  }
  .wip-dropdown.open {
    display: block;
  }

  /* ── WIP Items ──────────────────────────────────────── */
  .wip-item {
    margin-bottom: 10px;
    border-left: 3px solid var(--accent-soft);
    padding-left: 10px;
    transition: border-left-color 0.2s;
  }
  .wip-item:hover {
    border-left-color: var(--primary);
  }
  .wip-item-title {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 8px;
    cursor: pointer;
    padding: 4px 0;
  }
  .wip-item-title span {
    font-family: 'Lato', sans-serif;
    font-size: 0.88rem;
    font-weight: 700;
    color: var(--primary);
    line-height: 1.4;
  }
  .wip-chevron {
    font-size: 0.65rem;
    color: #aaa;
    flex-shrink: 0;
    margin-top: 4px;
    transition: transform 0.2s;
  }
  .wip-item.expanded .wip-chevron {
    transform: rotate(180deg);
    color: var(--primary);
  }
  .wip-item-details {
    display: none;
    font-family: 'Lato', sans-serif;
    font-size: 0.83rem;
    color: #555;
    padding: 5px 0 3px 0;
    line-height: 1.5;
  }
  .wip-item-details .wip-authors {
    color: #444;
  }
  .wip-item-details .wip-venue {
    display: block;
    font-style: italic;
    color: #888;
    margin-top: 2px;
  }
  .wip-item-details .wip-status {
    display: inline-block;
    margin-top: 5px;
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--primary);
    background: #e0e8f5;
    padding: 2px 7px;
    border-radius: 4px;
  }
  .wip-item.expanded .wip-item-details {
    display: block;
  }
</style>

<!-- _pages/publications.md -->

<!-- {% include bib_search.liquid %} -->

<!-- WIP Banner — sits right above the publications list -->
<div class="wip-banner" id="wipBanner" onclick="toggleWipDropdown()">
  <div class="wip-banner-left">
    <span class="wip-banner-icon">🔬</span>
    <span class="wip-banner-label">Work in Progress</span>
    <span class="wip-banner-hint">— click to view ongoing work</span>
  </div>
  <span class="wip-banner-chevron">▼</span>
</div>

<!-- WIP Dropdown -->
<div class="wip-dropdown" id="wipDropdown">

  <div class="wip-item" id="wip-1">
    <div class="wip-item-title" onclick="toggleWipItem('wip-1')">
      <span>Paper Title One</span>
      <span class="wip-chevron">▼</span>
    </div>
    <div class="wip-item-details">
      <span class="wip-authors">V. Thammanna Gowda, et al.</span>
      <span class="wip-venue">Target venue/journal.</span>
      <span class="wip-status">In Preparation</span>
    </div>
  </div>

  <div class="wip-item" id="wip-2">
    <div class="wip-item-title" onclick="toggleWipItem('wip-2')">
      <span>Paper Title Two</span>
      <span class="wip-chevron">▼</span>
    </div>
    <div class="wip-item-details">
      <span class="wip-authors">V. Thammanna Gowda, et al.</span>
      <span class="wip-venue">Target venue/journal.</span>
      <span class="wip-status">Under Review</span>
    </div>
  </div>

</div>

<div class="publications">

{% bibliography %}

</div>

<script>
  function toggleWipDropdown() {
    document.getElementById('wipBanner').classList.toggle('open');
    document.getElementById('wipDropdown').classList.toggle('open');
  }
  function toggleWipItem(id) {
    document.getElementById(id).classList.toggle('expanded');
  }
</script>